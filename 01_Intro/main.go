package main

import "fmt"

// the main entry point is the main function similar to c languages
func main() {
	// methods from the packages are always capitalized
	fmt.Println("Hello from Khushi Sharma.")
}

// lexer comes in and manually puts the semicolon at every place.

// lexer in every language so you are following the grammar rules of the language.